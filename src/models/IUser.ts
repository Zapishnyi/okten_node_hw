export default interface IUser {
  id: number;
  name: string;
  userName: string;
  password: string;
  age: number;
  email: string;
  gender: string;
  // eslint-disable-next-line prettier/prettier,semi
};
